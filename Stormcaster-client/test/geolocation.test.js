const assert = require('assert');
const { describe, it } = require('mocha');

describe('Convert coordinates to locale string', () => {
   describe('#coordinates', () => {
      it('Should return latitude and longitude', () => {
         assert.strictEqual();
      });
   });
});
