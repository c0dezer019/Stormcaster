import React from 'react';
import { Container } from 'react-bootstrap';

const UserMenu = () => {
    return (
        <Container>
            <h4>Menu</h4>
        </Container>
    );
};

export default UserMenu;
